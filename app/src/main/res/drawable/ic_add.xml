<?xml version="1.0" encoding="utf-8"?>
<!--
  Vector drawable. A vector is XML, not a PNG per density, so one file covers
  every screen size. Prefer vectors for icons - they are also diffable in git,
  which PNGs are not.

  24dp viewport with `tint` inherited from the button's theme colour.
-->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="24dp"
    android:height="24dp"
    android:viewportWidth="24"
    android:viewportHeight="24"
    android:tint="@color/white">
    <path
        android:fillColor="@android:color/white"
        android:pathData="M19,13h-6v6h-2v-6H5v-2h6V5h2v6h6v2z" />
</vector>
